[workspace]
members = ["sbepis", "jack_noir", "state_machine", "bevy_marching_cubes"]
default-members = ["sbepis"]
resolver = "2"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
