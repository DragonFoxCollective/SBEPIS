[workspace]
members = ["sbepis", "marching_cubes", "jack_noir", "state_machine"]
default-members = ["sbepis"]
resolver = "2"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
